# Local Development Environment Variables
# Copy this file to .env.local and adjust values for local development

# PostgreSQL (when running PostgreSQL locally, not in Docker)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/lisacbot
SPRING_DATASOURCE_USERNAME=lisacbot
SPRING_DATASOURCE_PASSWORD=lisacbot

# Bot Configuration (optional overrides)
# BOT_POLL_INTERVAL_SECONDS=30
# BOT_INITIAL_BALANCE=1000.0
