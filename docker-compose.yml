version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: lisacbot-postgres
    environment:
      POSTGRES_DB: lisacbot
      POSTGRES_USER: lisacbot
      POSTGRES_PASSWORD: lisacbot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lisacbot"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
